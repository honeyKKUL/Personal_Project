# name: Update Dev Folder for GitHub Pages

# on:
#   push:
#     branches:
#       - main # main 브랜치에 push될 때 자동 실행

# permissions:
#   contents: write

# jobs:
#   update-dev:
#     runs-on: ubuntu-latest

#     steps:
#       - name: 📥 Checkout repository
#         uses: actions/checkout@v4

#       - name: 🧹 Clean dev folder
#         run: |
#           rm -rf dev/*
#           mkdir -p dev

#       - name: 📦 Copy latest code into dev folder
#         run: |
#           cp -r index.html css js dev/

#       - name: 🧾 Commit and push changes
#         run: |
#           git config user.name "github-actions[bot]"
#           git config user.email "github-actions[bot]@users.noreply.github.com"

#           git add dev
#           # 커밋할 변경사항이 있을 경우에만 커밋하도록 처리
#           if ! git diff --cached --quiet; then
#             git commit -m "🔄 Update dev test version (auto)"
#             git push origin main
#           else
#             echo "No changes to commit."
#           fi

#       - name: ✅ Done
#         run: |
#           echo "✅ /dev 폴더가 업데이트되었습니다. https://honeykkul.github.io/Personal_Project/dev/"
